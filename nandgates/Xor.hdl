CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Nand(a=a, b=a,out=wa);
    Nand(a=b, b=b,out=wb);
    Nand(a=a, b=b,out=wab);

    Nand(a=wab, b=wab, out=w1);
    Nand(a=w1, b=w1, out=w2);

    Nand(a=wa, b=wb, out=w3);

    Nand(a=w3, b=w2, out=w4);
    Nand(a=w4, b=w4, out=out);
}
